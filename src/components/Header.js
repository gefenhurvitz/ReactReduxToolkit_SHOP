import React from "react";
import { Container } from "react-bootstrap";

const Header = () => {
  return (
    <header>
      <Container>
        <h1>My App</h1>
      </Container>
    </header>
  );
};

export default Header;
